<INSTRUCTIONS>
## 需求澄清规则

执行任务前先确认需求清晰；如有歧义或信息缺失，先向用户提问并持续澄清，明确目标后再执行。可由项目文件确认的信息优先自行核实。
</INSTRUCTIONS>

## 磁盘路径规则

项目环境、模型、缓存、运行输出和临时文件默认放在 `F:\`；仅系统强制或软件无法配置时使用 `C:\`，并在 `PROJECT.md` 记录例外。

## Git 规则

每次任务结束自动检查 Git；存在实质且已验证的改动时自动提交。形成阶段性进展且远端可用时自动推送 GitHub；临时、未验证、大文件或敏感内容不得提交或推送。

## CARLA 运行规则

1. CARLA 实机任务优先使用服务器；仅在 SSH、RPC 或健康检查失败时回退本机。
2. 服务器和本机统一使用 CARLA `0.9.16` 及项目专用 Anaconda 环境 `Carla666-0916`；禁止使用系统 Python 或其他环境运行实机任务。
3. 本机环境固定为 `D:\ANACONDA\envs\Carla666-0916`，CARLA 程序固定为 `F:\Carla\carla-0.9.16`；运行前必须确认客户端/服务端版本一致，否则停止任务。
4. CARLA `0.9.15` 不再作为程序、环境或 Python 包使用；历史证据文件可保留，但不得作为运行输入。
5. 结论依赖运行行为时必须连接真实 CARLA 验证；静态校验或 mock 不得替代实机证据。
