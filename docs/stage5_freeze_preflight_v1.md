# 阶段五 V1.0 冻结前检查 V1

_检查类型：静态、离线和可重建材料检查；不启动 CARLA，不检查 GPU，不等同于软著提交审批_

## 检查入口

在项目根目录执行：

```powershell
tools\check_stage5_freeze.cmd
```

正式冻结提交前，工作区应当干净，再执行：

```powershell
tools\check_stage5_freeze.cmd --require-clean
```

检查器位于 `tools/check_stage5_freeze.py`，只使用项目文件、M08 清单、质量门配置和 `Carla666-0916` 的 Python API 导入检查，不连接 CARLA RPC，不启动训练任务。

## 当前检查项

| 类别 | 检查内容 | 通过含义 |
|---|---|---|
| 文件 | 阶段四报告、阶段五目录、软著台账、模块映射、接口规格存在 | 材料入口没有缺失 |
| 口径 | 软件名称 V1.0、M01–M08、当前阶段五表述 | 当前文档没有把旧阶段状态当作现状 |
| M08 清单 | 格式、离线模式、CARLA 边界、M02/M04/M07/M08 状态 | 一键演示契约满足 |
| 场景库计数 | M03/M07 的 `117` 条目和 `351` 条严格验收来源证据 | 与质量门配置一致 |
| 输出物 | 输入副本、编译配置、`.xosc`、`.carla.json`、适配清单、主清单 | 当前演示产物可追溯 |
| 清单哈希 | `demo_manifest.json` SHA-256 与材料索引一致 | 索引没有指向另一份清单 |
| 运行环境 | `D:\ANACONDA\envs\Carla666-0916`、`F:\Carla\carla-0.9.16`、CARLA Python API 导入 | 运行基线路径存在且客户端 API 可加载 |
| Git | 可选 `--require-clean` | 冻结前没有未提交改动 |

## 本轮结果

2026-08-23 在当前工程基线上运行 `tools\check_stage5_freeze.cmd`：`30 PASS / 0 FAIL / 3 PENDING`；提交后使用 `--require-clean` 为 `31 PASS / 0 FAIL / 3 PENDING`。这表示当前工程可以进入“指定最终提交并执行冻结”的操作阶段，但尚未完成正式 V1.0 封版。

## 当前结果解释

检查器全部 `PASS` 只表示工程材料达到“可以进入 V1.0 冻结操作”的条件，不表示最终版本已经冻结。每次正式申请前仍需人工完成以下 `PENDING` 项：

1. 指定最终 V1.0 提交、软件名称和功能范围；
2. 在该提交上重新运行全量测试、`compileall` 和 M08，并更新清单哈希；
3. 复核已归档的 S01–S09 Web 截图，必要时在冻结提交上重新采集，并确保截图来自同一冻结提交；
4. 确认申请主体、著作权人、开发完成日期和当期官方材料格式。

任何 `FAIL` 都应先修复再进入冻结；`PENDING` 不得被表述为已完成的软著申请材料。
