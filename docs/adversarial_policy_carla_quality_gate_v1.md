# 阶段四独立策略质量门审计 V1

_完成日期：2026 年 8 月 22 日；范围：首轮 12 个独立 pair 与扩展 6 个独立 pair_

## 审计范围

本审计读取首轮独立评估和未覆盖分层扩展的 `run_results.json`，合计 `18` 个独立 pair、`54` 条 CARLA 运行。3 个原始 pair 的 Traffic Manager 重复种子不计入独立样本。扩展运行证据目录为 `F:\Carla\project-transfer\server-results\20260822_135901_20260822_141306`。

## 硬质量门

| 质量门 | 结果 | 判定 |
|---|---:|---|
| 严格验收 | `54/54` | 通过 |
| CARLA 客户端/服务端版本匹配 | `54/54`，均为 `0.9.16` | 通过 |
| RGB 传感器写盘 | `54/54`，每次 `100` 帧 | 通过 |
| CARLA 服务健康 | `54/54` | 通过 |
| 风险方法 | `54/54` 为 `heuristic_v2` | 通过 |
| 路线验收 | `route_verified=54/54` | 通过 |
| 全程路线状态 | `53/54` 为 full-run；`1/54` 为碰撞前验收 | 需记录边界，不构成门失败 |

唯一的碰撞前验收运行是扩展批次 `CVAE/critical` 的 SAC 候选。该运行碰撞前主车和前车在途率均为 `1.0`，最大碰撞前路线偏差为 `0.992 m`；碰撞后主车偏离路线至 `8.319 m`，不能把碰撞后的状态误写成路线控制器在完整运行中的通过证据。

扩展批次候选没有无效尝试或运行失败；首轮的 2 次 rule-guided LHS 无效尝试均已恢复，最终 `54/54` 运行仍严格通过。

## 碰撞分层复核

下表按独立 pair 汇总碰撞运行数，不把连续碰撞回调次数当作独立事故数：

| 目标档 | 独立 pair | 共享基线 | SAC | rule-guided LHS |
|---|---:|---:|---:|---:|
| low | 3 | 0 | 0 | 0 |
| medium | 3 | 0 | 0 | 1 |
| high | 6 | 1 | 2 | 3 |
| critical | 6 | 4 | 4 | 2 |

碰撞集中在 `high/critical` 分层，与本轮优先补齐高风险独立集合的采样目的相符。扩展批次中，critical 的基线/SAC/LHS 碰撞数分别为 `2/3`、`3/3`、`2/3`，high 分别为 `0/3`、`1/3`、`2/3`；这说明新增候选确实增加了碰撞状态变化观察，但没有出现服务、传感器或路线质量门的系统性崩溃。

候选目标档命中为 `19/36`。`target_risk_level` 是生成与选择的设计条件，不是真实风险标签；未命中不能替代 CARLA 严格验收，也不能单独判定运行失败，实测风险应以 `observed_risk` 为准。

## 决策

本轮没有硬质量门回归，不重复已完成场景，不增加 SAC 训练预算，不启动 CARLA 在线训练。若后续需要重复 Traffic Manager 种子，优先关注扩展 `CVAE/critical` 的 SAC 碰撞后路线偏离，以及 `LHS/high` 两个候选均发生碰撞的 pair；重复种子仍是同一独立场景的重复测量。

