# 基于生成式AI的自动驾驶极端场景库构建与仿真测试平台

> 项目进度文档 | 更新日期：2026-06-18

---

## 一、项目概述

构建一个面向自动驾驶的**极端场景库**，利用生成式 AI 生成极端驾驶场景（恶劣天气、危险交通流、异常事件），并在 CARLA 仿真平台中进行测试验证。

**技术路线**：CARLA 仿真 → 场景参数化 → 生成式 AI 场景生成 → 多传感器数据采集 → 模型训练与评估

---

## 二、技术架构

```
┌─────────────────────────────────────────────────┐
│              生成式 AI 场景生成                   │
│        (服务器端：NVIDIA L20 / 46 GB VRAM)        │
├─────────────────────────────────────────────────┤
│               CARLA 仿真引擎                      │
│   ┌──────────────┐   ┌──────────────────────┐   │
│   │ 笔记本端      │   │ 服务器端（待恢复）     │   │
│   │ RTX 4060     │   │ 无渲染批量仿真         │   │
│   │ 可视化调试    │   │ 大规模数据生成         │   │
│   └──────────────┘   └──────────────────────┘   │
├─────────────────────────────────────────────────┤
│              场景评估与测试                       │
│         OpenSCENARIO / ScenarioRunner            │
└─────────────────────────────────────────────────┘
```

---

## 三、环境配置（已完成）

### 3.1 笔记本端（当前可用）

| 项目 | 详情 |
|------|------|
| 操作系统 | Windows 10/11 |
| GPU | NVIDIA RTX 4060（8 GB VRAM） |
| CARLA 版本 | 0.9.15（预编译包） |
| CARLA 路径 | `F:\Carla\WindowsNoEditor` |
| 启动方式 | 双击 `CarlaUE4.exe` |
| Python 版本 | 3.10.9 |
| Python API | `carla==0.9.15`（pip 安装） |
| Python 包 | `pygame`, `numpy` |

### 3.2 服务器端（暂不可用，待恢复）

| 项目 | 详情 |
|------|------|
| 连接方式 | VSCode SSH → gpu03 |
| 操作系统 | Ubuntu 22.04.5 LTS |
| GPU | NVIDIA L20（46 GB VRAM） |
| 驱动版本 | 580.159.03 / CUDA 13.0 |
| Conda 环境 | `carla`（Python 3.10） |
| 工作目录 | `/data/zzr/carla_env` |
| CARLA 包 | 已下载 `CARLA_0.9.15.tar.gz` |

---

## 四、文件结构

```
F:\Carla\
├── WindowsNoEditor\          # CARLA 0.9.15 预编译包
│   ├── CarlaUE4.exe          # 启动程序
│   └── PythonAPI\            # API 和示例
│       ├── carla\            # Python 客户端库
│       └── examples\         # 官方示例脚本
│           ├── manual_control.py
│           ├── generate_traffic.py
│           └── ...
├── test\                     # 自定义场景脚本
│   ├── scene_01_extreme_weather.py   # 场景1：极端天气
│   └── output\               # 传感器输出（自动创建）
└── ...
```

---

## 五、已完成进度

| 序号 | 任务 | 状态 |
|------|------|------|
| 1 | 环境调研与文档整理 | ✅ |
| 2 | 服务器 CARLA 环境准备（Conda + 下载包） | ✅ |
| 3 | 笔记本 CARLA 安装与启动 | ✅ |
| 4 | Python API 安装与连接验证 | ✅ |
| 5 | `manual_control.py` 手动操控 | ✅ |
| 6 | `generate_traffic.py` 交通流生成 | ✅ |
| 7 | 自定义场景脚本：极端天气 + 车辆 + 相机 | ✅ |

---

## 六、后续计划

### 阶段 1：场景开发（当前）
| 任务 | 说明 |
|------|------|
| 多危险叠加场景 | 前车急刹 + 行人横穿 + 恶劣天气 |
| 多传感器集成 | LiDAR、深度相机、语义分割相机 |
| 场景参数化 | 配置文件驱动，批量生成变体 |

### 阶段 2：数据采集
| 任务 | 说明 |
|------|------|
| 传感器数据录制 | RGB + LiDAR + 深度 + 语义 |
| 数据集构建 | 极端场景图片/点云数据集 |
| 数据标注 | 危险等级、场景类型标签 |

### 阶段 3：AI 集成
| 任务 | 说明 |
|------|------|
| 服务器环境恢复 | 无渲染 CARLA + 模型训练环境 |
| 生成式 AI 模型 | 训练场景生成模型 |
| 场景自动生成 | AI 自动创建极端场景变体 |

### 阶段 4：测试与评估
| 任务 | 说明 |
|------|------|
| ScenarioRunner 集成 | OpenSCENARIO 格式场景导入 |
| 自动驾驶算法测试 | 在极端场景下评估性能 |
| 报告撰写 | 项目总结与论文 |

---

## 七、速查命令

### 笔记本端
```cmd
# 启动 CARLA
双击 F:\Carla\WindowsNoEditor\CarlaUE4.exe

# 运行自定义场景
cd F:\Carla\test
python scene_01_extreme_weather.py

# 运行官方示例
cd F:\Carla\WindowsNoEditor\PythonAPI\examples
python manual_control.py        # 手动开车（注意切英文输入法）
python generate_traffic.py -n 30  # 生成交通流
python automatic_control.py       # 自动驾驶演示

# 安装依赖
pip install carla==0.9.15 pygame numpy
```

### 服务器端（待恢复）
```bash
conda activate carla
cd /data/zzr/carla_env/CARLA_0.9.15
./CarlaUE4.sh -RenderOffScreen -quality-level=Low
```

### CARLA Python API 常用片段
```python
import carla

# 连接
client = carla.Client("localhost", 2000)
world = client.get_world()

# 天气
world.set_weather(carla.WeatherParameters(cloudiness=100, precipitation=100, fog_density=80))

# 生成车辆
bp = world.get_blueprint_library().filter("vehicle.*")[0]
sp = world.get_map().get_spawn_points()[0]
vehicle = world.spawn_actor(bp, sp)

# 自动驾驶
vehicle.set_autopilot(True)

# 清理
vehicle.destroy()
```
---

## 八、关键链接

| 资源 | 地址 |
|------|------|
| CARLA 官方文档 | https://carla.readthedocs.io/ |
| CARLA GitHub | https://github.com/carla-simulator/carla |
| CARLA Releases | https://github.com/carla-simulator/carla/releases |
| ScenarioRunner | https://github.com/carla-simulator/scenario_runner |
| OpenSCENARIO | https://www.asam.net/standards/detail/openscenario/ |
